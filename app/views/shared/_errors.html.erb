<% if obj.errors.any? %>
  <div class="error_container">
    <h2>  <%= pluralize(obj.errors.count, "error") %> prevented the article from being saved</h2>
    <ul class="list-group">
      <% obj.errors.full_messages.each do |msg| %>
        <li class="list-item"><%= msg %></li>
      <% end %>
    </ul>
  </div><br>
<% end %>
